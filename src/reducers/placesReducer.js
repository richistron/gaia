const initialState = {
};

const placesReducer = (state, action) => {
  if (!state) {
    return initialState;
  }

  switch (action.type) {
    default:
      return state;
  }
};

export default placesReducer;
